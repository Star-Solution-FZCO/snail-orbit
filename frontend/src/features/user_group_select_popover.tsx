import { memo } from "react";

type UserGroupSelectPopoverProps = {};

export const UserGroupSelectPopover = memo(
    (props: UserGroupSelectPopoverProps) => {},
);

UserGroupSelectPopover.displayName = "UserGroupSelectPopover";
